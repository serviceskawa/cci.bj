
import { createStore } from "vuex";

const store = createStore({
  state: {
    news: [],
    show_alert: true,
    configs: {},
    faqs: [],
    home_elements: {}
  },
  getters: {},
  actions: {
  },
  mutations: {
  },
  modules: {},
});

export default store;
